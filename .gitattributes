# 换行统一：仓库内一律存 LF，检出时也用 LF，不受各人 core.autocrlf 影响
* text=auto eol=lf

# 二进制资源不做换行转换，也不参与文本 diff
*.png binary
*.ico binary
*.icns binary

# 主题字体。git 的启发式（前 8KB 有 NUL 即算二进制）本来也能兜住，
# 但显式标掉更稳：哪天某个 woff2 的头 8KB 恰好没有 NUL 就会被当文本改坏。
*.woff2 binary
*.woff binary
*.ttf binary
*.otf binary

# crab-inject.ps1 也按 LF 存与检出（PowerShell 不在乎行尾）。
# 这里不能用 eol=crlf：themes/manifest.json 的 sha256 是按工作区字节算的，而在线更新
# 从 raw.githubusercontent.com 拿到的是仓库里存的 LF 字节。一旦某个文件在检出时被转成
# CRLF，在那台机器上重跑生成器就会写进一份 CRLF 的哈希，用户端校验必然失败。
# 保持「仓库字节 == 检出字节 == manifest 哈希」这一条不变量，比行尾好看重要。
*.ps1 text eol=lf
